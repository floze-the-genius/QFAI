# 05 Examples

## Purpose

- Concretize BR into executable examples.
- Every EX must reference at least one BR via `BR-Ref`.
- One EX per BR, index-aligned: the example whose trailing number is _n_ concretizes the business
  rule whose trailing number is _n_, for every rule in `04_Business-Rules.md`. The 1:1 mapping is
  deliberate: `04_Business-Rules.md` § `Granularity (required)` already folds a rule's
  planted-violation form into the same BR as its positive assertion, so a second EX per rule would
  split an oracle that the rule keeps together.
- `Input` is what a test sets up or reads; `Expected` is the observable that falsifies the rule
  when it disagrees. Where the rule is a **sequencing** or **record-keeping** obligation, the
  observable is a repository artifact (`07_Decisions.md`, the merge order, the evidence quoted in a
  pull-request description) rather than a program output — those rules are violable by merge order
  or by omission, not by a file's contents.

## Scope reminder

Every example below reads or plants against **QFAI's own** toolchain — `.github/workflows/**`,
`.github/actions/**`, root `scripts/**`, `packages/qfai/scripts/**` and the test-runner
configuration. Where an example names the shipped tree
(`packages/qfai/assets/init/root/.github/workflows/**`) it is an input to the **lane that scans
it**; the files themselves are `spec-0003`'s, per
`.qfai/contracts/cli/shipped-workflows.md` §1 / §3. No example here asserts what a shipped file
must contain except through the hygiene lane's shipped-file rules (`CLI-WFSET` §6).

## Example Table (required)

| EX-ID        | BR-Ref       | Input                                                                                                                                                                   | Expected                                                                                                                                                                                                                                                                                        |
| ------------ | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0017-0001 | BR-0017-0001 | The aggregate verdict job's body, plus a tree in which one job has been wired into its `needs` and nothing else edited                                                  | The body iterates the serialized needs map and holds no hand-written list of need names; the added need is evaluated with a zero-line diff to the verdict body                                                                                                                                  |
| EX-0017-0002 | BR-0017-0002 | Two synthetic needs maps: one where every need concluded `success`, one where every need concluded `skipped`                                                            | The verdict exits 0 for both. "Nothing needed running" and "everything passed" are the two accepting states                                                                                                                                                                                     |
| EX-0017-0003 | BR-0017-0003 | Synthetic needs maps holding `failure`, holding `cancelled`, and holding a state token the verdict does not recognize                                                   | Exit 1 in all three. The unrecognized token is rejected because the accepting set is closed, not because it was mapped to a rejecting value                                                                                                                                                     |
| EX-0017-0004 | BR-0017-0004 | The aggregate verdict job's key and display name, read before and after every change this spec makes                                                                    | The string is identical at every point. A rename shows as a diff on the job key, which is the falsifying observation                                                                                                                                                                            |
| EX-0017-0005 | BR-0017-0005 | A proposed merge in which a job is added to the own-CI workflow while the verdict still enumerates its needs by hand                                                    | Rejected in review, citing DR-0017-0005 edge 1; in the accepted history the derived-verdict change is an ancestor of every job-adding change                                                                                                                                                    |
| EX-0017-0006 | BR-0017-0006 | The test matrix definition after lane selection lands, and a run whose detection output excludes one leg                                                                | Every leg is still declared and carries a detection-derived condition; the excluded leg reports `skipped`, its check name persists, and it consumes no runner minutes. No leg is removed from the matrix list                                                                                   |
| EX-0017-0007 | BR-0017-0007 | A pull request touching only Markdown outside the recognized source directories                                                                                         | At most four job instances execute while the job named `build` carries the required status context, and at most three once that context moves. The four are detection, lint, build and the verdict                                                                                              |
| EX-0017-0008 | BR-0017-0008 | The change-detection step run against (a) a full-history clone with a reachable base ref and (b) a shallow clone and an unreachable base ref                            | (a) selects the narrowed lane set and emits no annotation; (b) emits an explicit warning annotation naming the reason and selects the full lane set                                                                                                                                             |
| EX-0017-0009 | BR-0017-0009 | A change list whose only entry is a path inside no recognized directory                                                                                                 | The full lane set is selected. An unclassified path is never read as "nothing to run"                                                                                                                                                                                                           |
| EX-0017-0010 | BR-0017-0010 | Two change lists: one touching only Markdown under the assistant catalog tree, one touching only the agent-integration mirrors                                          | The assistant-tree change selects the full lane set because it alters validate output; the mirrors change classifies as documentation-only                                                                                                                                                      |
| EX-0017-0011 | BR-0017-0011 | A detection output that selects zero test lanes                                                                                                                         | The lint lane still executes, so the formatter, the Markdown linter, the leakage guard and the branch-version-pin guard all run on a documentation-only pull request                                                                                                                            |
| EX-0017-0012 | BR-0017-0012 | The job named in the required-status-context declaration and its transitive `needs` closure, with a condition planted on one job it depends on                          | The hygiene lane exits 1 and reports the declared job as skippable, even though that job carries no condition of its own                                                                                                                                                                        |
| EX-0017-0013 | BR-0017-0013 | The set of check names a run reports, derived before lane selection lands and after it lands                                                                            | The two sets are identical: no check name is created, removed or renamed, so no repository setting has to change                                                                                                                                                                                |
| EX-0017-0014 | BR-0017-0014 | Two fixture workflows: one job with no job-level `permissions:` inside a workflow that declares one, and one job with neither                                           | The reachability counter accepts the first and rejects the second. A declaration-only counter rejects both, which is how the two measurements are told apart — 4 of 12 reachable against 2 of 12 declared at the baseline                                                                       |
| EX-0017-0015 | BR-0017-0015 | Every job across `.github/workflows/**` after the own-tree hardening                                                                                                    | Zero jobs have no reachable permission block, and every job this spec adds carries both a reachable permission block and a `timeout-minutes`                                                                                                                                                    |
| EX-0017-0016 | BR-0017-0016 | The permission blocks that depart from the minimal-scope default                                                                                                        | Exactly two, both preserved as declared exceptions: the aggregate verdict's empty permission map and the publishing job's identity-token write. A third is a new elevation needing its own justification                                                                                        |
| EX-0017-0017 | BR-0017-0017 | One job with both its job-level and its workflow-level permission block removed, then each of the two restored in turn                                                  | Exit 1 naming the workflow and the job while both are absent; exit 0 after restoring either one. Restoring the workflow-level block alone is sufficient                                                                                                                                         |
| EX-0017-0018 | BR-0017-0018 | Every checkout step across `.github/workflows/**`, then a copy with `persist-credentials: false` deleted from exactly one step                                          | The count of steps not setting the flag is 0 on the clean tree; the planted tree exits 1 and names the offending step's file and job                                                                                                                                                            |
| EX-0017-0019 | BR-0017-0019 | The two jobs that legitimately need full history, plus every workflow-level checkout default                                                                            | Full-history checkout is requested on those two jobs only; no workflow-level default requests it                                                                                                                                                                                                |
| EX-0017-0020 | BR-0017-0020 | Every `uses:` value across `.github/workflows/**` and `.github/actions/**`, then a copy with one value replaced by a floating major-version reference                   | All values resolve to a forty-hex commit SHA on the clean tree; the planted tree exits 1 naming that reference                                                                                                                                                                                  |
| EX-0017-0021 | BR-0017-0021 | A conventional readable version trailer beside an own-CI pin, plus the diff of the distributed-surface guard set for the same change                                    | `check-no-internal-version-leakage.sh` exits 0 because its root resolves to the package directory, and the guard's pattern set, pragma handling and allow-list show a zero-line diff                                                                                                            |
| EX-0017-0022 | BR-0017-0022 | Every durable repository artifact searched for a named action-pin bump owner                                                                                            | DR-0017-0003 in `07_Decisions.md` names the owner as a maintainer role and binds the obligation to release preparation. A bump owner stated only in a pull-request description is not accepted, because no gate can read one                                                                    |
| EX-0017-0023 | BR-0017-0023 | The repository root after this spec's changes, plus the recorded reason for the absence of a bump configuration                                                         | No automated action-bump configuration was created by any agent, and the bump owner is recorded anyway. The two halves are independent — the owner is recordable today, the configuration is not                                                                                                |
| EX-0017-0024 | BR-0017-0024 | Occurrences of the frozen-lockfile install literal in the own-CI workflow and in the shared setup definition                                                            | Zero in the workflow and exactly one in the shared definition, down from six duplications in one file                                                                                                                                                                                           |
| EX-0017-0025 | BR-0017-0025 | Every own-CI job that needs the toolchain                                                                                                                               | Each consumes the shared setup definition; zero jobs restate any step of the preamble inline                                                                                                                                                                                                    |
| EX-0017-0026 | BR-0017-0026 | The shared setup definition's step list, in order                                                                                                                       | Package-manager shim enabled, Node set up with the package-manager cache and an explicit cache-dependency path, package manager re-shimmed against the toolcache Node, frozen-lockfile install. The re-shim step is present                                                                     |
| EX-0017-0027 | BR-0017-0027 | The own workflows tree searched for a workflow-level Node version literal, plus the shared definition's version source                                                  | Zero workflow-level Node version literals remain, and the shared definition reads the version from a file in the repository                                                                                                                                                                     |
| EX-0017-0028 | BR-0017-0028 | The packaged asset tree searched for the shared setup definition, plus a run of the pack verifier                                                                       | The definition is absent from `packages/qfai/assets/init/**`, and pack verification still throws on any child other than `workflows` under the shipped GitHub configuration                                                                                                                     |
| EX-0017-0029 | BR-0017-0029 | Bundler build invocations per pull request, counted before and after artifact reuse, against the captured baseline                                                      | The build is produced once, uploaded, and downloaded by the legs that rebuild today; the recorded invocation count falls against the baseline; the two builds the pack-verification lifecycle fires are unchanged                                                                               |
| EX-0017-0030 | BR-0017-0030 | A cost, wall-clock or parallelism change proposed with no captured before-and-after numbers                                                                             | Rejected. The accepting shape is numbers written to the evidence tree **and** quoted in the pull-request description **and** in `07_Decisions.md`, because the evidence tree is ignored by git                                                                                                  |
| EX-0017-0031 | BR-0017-0031 | A captured before-and-after measurement showing a wall-clock regression from artifact reuse                                                                             | The rebuilds are kept and the measurement is recorded as the reason. That record satisfies the requirement; a second comparison run to obtain a different answer is forbidden                                                                                                                   |
| EX-0017-0032 | BR-0017-0032 | The own-CI workflow after the producer and verifier split, read for the job named `build`                                                                               | A job of that exact name exists, carries no condition of its own, no job it depends on carries one, and every item of its enumerated verification set is still performed by it or by a job it needs                                                                                             |
| EX-0017-0033 | BR-0017-0033 | Every step of the `build` job that belongs to its enumerated verification set                                                                                           | No such item carries `continue-on-error`. An item that keeps running while losing the ability to fail the job is a weakened item, not a preserved one                                                                                                                                           |
| EX-0017-0034 | BR-0017-0034 | The report upload step, plus fixture steps with retention 7 and retention 8 and an unconditional run condition                                                          | The real step skips on cancellation, tolerates a missing report file and sets retention to at most 7 days; the retention-8 fixture and the unconditional fixture are both rejected                                                                                                              |
| EX-0017-0035 | BR-0017-0035 | The file count under `.github/workflows/` and the aggregate check name, before and after test-layer separation                                                          | Both unchanged; the layer split is expressed as jobs and matrix legs inside the existing file, and a new per-layer workflow file is rejected in review                                                                                                                                          |
| EX-0017-0036 | BR-0017-0036 | Every layer token and layer heading extracted from the files the layer-policy loader reads, plus the built-in token set                                                 | The five declared tokens are unchanged and the built-in set is unmodified. No new layer, layer token or layer heading is introduced, and extending the built-in set to legalize CI vocabulary is rejected                                                                                       |
| EX-0017-0037 | BR-0017-0037 | The hygiene lane run over the hardened own workflows tree                                                                                                               | Exit 0, with exactly five rules asserted: permissions and `timeout-minutes` per job, checkout credential refusal, SHA-pinned action references, matrix `fail-fast` disabled, and zero secret inheritance                                                                                        |
| EX-0017-0038 | BR-0017-0038 | The hygiene lane's output after a run that exited 0                                                                                                                     | The output enumerates each rule the run evaluated. A rule that was not evaluated is absent from that list rather than implied by the green result                                                                                                                                               |
| EX-0017-0039 | BR-0017-0039 | A positive and a negative fixture for each hygiene rule, with exactly one rule's violation planted at a time                                                            | Exit 1 naming the offending file, the offending job and the rule identifier; removing the planted violation returns the lane to exit 0. An aggregate fixture cannot show which rule fired                                                                                                       |
| EX-0017-0040 | BR-0017-0040 | The failure the hygiene lane emits, and the reviewer-finding justification catalog                                                                                      | The failure is emitted as `R-WORKFLOW-HYGIENE-DRIFT` in the bare lint-aggregate namespace. Catalog membership is a separate, severity-class question deferred under `OQ-0015-0001`; the code's present absence from the catalog is a temporary divergence and must not be asserted as permanent |
| EX-0017-0041 | BR-0017-0041 | The lint aggregate's and the release-only gate aggregate's script definitions, plus a planted violation on a pull request                                               | The hygiene lane's invocation appears in the lint aggregate or in a runner project the test matrix drives, is absent from the release-only aggregate, and the planted violation turns the pull request red                                                                                      |
| EX-0017-0042 | BR-0017-0042 | The hygiene script's inputs, and the declaration naming the job expected to carry the required status context                                                           | The declaration is read from the working tree as a checked-in file; the script makes no call to live repository settings, which a pull request could not perform anyway                                                                                                                         |
| EX-0017-0043 | BR-0017-0043 | Three planted states: the declared context resolving to no job; the declared job made skippable through a job it depends on; one item removed from its verification set | Exit 1 in each of the three. Each has its own quiet failure mode, so one property standing in for the other two is not sufficient                                                                                                                                                               |
| EX-0017-0044 | BR-0017-0044 | The hygiene lane pointed at both the own workflows tree and the shipped workflows tree, then a violation planted in a shipped file only                                 | The clean pair exits 0; the planted case exits 1 and names the shipped path rather than an own-CI path. The scanned shipped files stay owned by the init capability                                                                                                                             |
| EX-0017-0045 | BR-0017-0045 | A change that enables the shipped-tree scan while the shipped tree is still unhardened                                                                                  | Rejected in review, because it lands instantly red. The accepting order is shipped-tree coverage in the same change as the shipped hardening or later, never before it                                                                                                                          |
| EX-0017-0046 | BR-0017-0046 | The shipped third-party rule evaluated against the one sanctioned package-manager setup action and against an unsanctioned third-party reference                        | The sanctioned entry passes and the unsanctioned reference exits 1. A rule expressed as a count of zero fails on the sanctioned entry, which is why that formulation is rejected                                                                                                                |
| EX-0017-0047 | BR-0017-0047 | Every project declared in the runner workspace                                                                                                                          | Each declares a pool and its pool options, a worker setting, a within-file concurrency setting, a file-parallelism setting and a hook timeout, against a baseline of name, include pattern and shared timeout only                                                                              |
| EX-0017-0048 | BR-0017-0048 | The declared starting values on the worker axis and the within-file concurrency axis, plus the override path for each                                                   | Ten on both axes, and each value stays overridable rather than fixed. The structure lands with the starting value; only the final value is measurement-gated                                                                                                                                    |
| EX-0017-0049 | BR-0017-0049 | A timing artifact comparing at least two worker settings on the largest project, plus the value actually adopted                                                        | The adopted setting is the fastest measured, or within ten percent of it with a written reason. A value adopted against no comparison does not satisfy the rule                                                                                                                                 |
| EX-0017-0050 | BR-0017-0050 | A measurement in which the higher worker setting is slower, or flakier, than the lower one                                                                              | The lower setting is kept and the measurement is recorded as the reason. A regression is a recorded outcome, not the start of a retry loop                                                                                                                                                      |
| EX-0017-0051 | BR-0017-0051 | An agent proposing a starting value other than ten on the strength of its own measurement                                                                               | Rejected without the user's sign-off. The measurement decides the final value; it does not authorize rewriting the declared starting point                                                                                                                                                      |
| EX-0017-0052 | BR-0017-0052 | The runner workspace searched for any retry setting, including after a tuning change that surfaced a race                                                               | Zero results. A retry is forbidden even to stabilize a tuning change, because it would mask the concurrent-writer races more workers can surface                                                                                                                                                |
| EX-0017-0053 | BR-0017-0053 | A parallelism tuning pull request and its recorded aggregate-verdict runs                                                                                               | Exactly one runner project is tuned, largest first, and three consecutive green aggregate-verdict runs are recorded with their run identifiers quoted in the description                                                                                                                        |
| EX-0017-0054 | BR-0017-0054 | Default-branch aggregate-verdict runs after a tuning change has merged                                                                                                  | A rerun-to-green rate above one in twenty reopens that setting. Three greens before merge do not close the budget permanently                                                                                                                                                                   |
| EX-0017-0055 | BR-0017-0055 | The declared runner project that matches zero files, is absent from the CI matrix and would fail an unfiltered run                                                      | Deleted, and its name no longer resolves as a project selector. It is referenced by zero spec items, so the removal cuts no downstream reference                                                                                                                                                |
| EX-0017-0056 | BR-0017-0056 | The two CI matrix slices that have no corresponding per-slice script                                                                                                    | Each gains one, and the matrix stops passing a project name through a generic script — the workaround that let the three surfaces drift apart unobserved                                                                                                                                        |
| EX-0017-0057 | BR-0017-0057 | The runner project set, the CI matrix slice list and the per-slice script set                                                                                           | All three hold the same names as each other, and each holds seven names once the dead project is deleted and the two missing scripts are added                                                                                                                                                  |
| EX-0017-0058 | BR-0017-0058 | The workflows directory listing and the trigger set of every remaining workflow                                                                                         | The repository's own duplicate of the shipped validate workflow is absent, and exactly one workflow is triggered by a pull request                                                                                                                                                              |
| EX-0017-0059 | BR-0017-0059 | The folded full-profile validate invocation, plus the alternative that repoints it at the shipped workflow file                                                         | The run executes from the `build` job against the repository root using the locally built binary. The repoint is rejected: the root manifest declares no dependency on the package, so it would resolve to the published one                                                                    |
| EX-0017-0060 | BR-0017-0060 | The `build` job's enumerated verification set after the fold                                                                                                            | The folded full-profile run is an item of that set, so removing it later is a release blocker rather than a cleanup                                                                                                                                                                             |
| EX-0017-0061 | BR-0017-0061 | A change deleting the duplicate validate workflow while no structural contract gate over the shipped set exists                                                         | Rejected in review. The accepting shape has the gate present in the same change or an earlier one, with the recorded justification being the loss of the manual cross-check, not the absence of a mirror that was already absent                                                                |
| EX-0017-0062 | BR-0017-0062 | The packaged asset catalog directory and the mirrored repository-root catalog directory, plus the layer catalog's links                                                 | The mapping file exists in both trees, the layer catalog cross-links to it and it cross-links back. A sibling file is invisible to the layer-policy parser; a new section inside the layer catalog is not                                                                                       |
| EX-0017-0063 | BR-0017-0063 | The mapping document's header block                                                                                                                                     | It states that the layer-policy loader does not read this file, so a later reader cannot mistake it for enforced vocabulary                                                                                                                                                                     |
| EX-0017-0064 | BR-0017-0064 | The mapping document's prose, read against the layer catalog's statement that per-level routing is a target state that is not enforced                                  | The prose does not read as activating per-level routing, and it does not instruct a reader to place a `TC-*` annotation outside the directory the live gate scans                                                                                                                               |
| EX-0017-0065 | BR-0017-0065 | The layer-vocabulary warning occurrence count before and after the mapping document lands, the built-in token set, and every layer code the document names              | The count equals the recorded baseline, the built-in token set is unmodified, and every layer code the document names also appears in the layer catalog                                                                                                                                         |
| EX-0017-0066 | BR-0017-0066 | Two candidate changes: one authoring the mapping document under the packaged asset catalog and running the mirror sync, one editing only the repository-root copy       | The first leaves the mirror script reporting no stale file and the tracked-tree diff exiting 0; the second is reverted by the next synchronization and fails that diff, so it is rejected                                                                                                       |
