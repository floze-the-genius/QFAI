# 07 Constraints

## Technical Constraints

| ID    | Constraint                                                                                                                                                                         | Rationale                               | Impact                                                   |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | -------------------------------------------------------- |
| TC-01 | Node.js >= 18.0.0                                                                                                                                                                  | current runtime baseline                | 実行環境の制約                                           |
| TC-02 | TypeScript validators は deterministic async 関数 `(root, config) => Promise<Issue[]>` に従う                                                                                      | validate repeatability                  | バリデータ実装の制約                                     |
| TC-03 | downstream execution skills は discussion pack を直接読まない                                                                                                                      | contract-first downstream を維持する    | `/qfai-sdd` 以降は specs + contracts only                |
| TC-04 | `/qfai-sdd` は discussion UIUX sidecar を `.qfai/contracts/design/**` と `.qfai/contracts/ui/**` に正規化する                                                                      | downstream truth source を固定する      | SDD preflight / generation の制約                        |
| TC-05 | repo-root `qfai validate` は contract-first validator 群を primary path にする                                                                                                     | current code posture に一致させる       | discussion direct-pack validation と分離                 |
| TC-06 | `runCanonicalUixValidators` は direct discussion-pack validation 専用                                                                                                              | latest discussion pack の暗黙解決を防ぐ | repo-root downstream validate では primary path にしない |
| TC-07 | declared screen ごとの screenshot / HTML snapshot は両方 mandatory                                                                                                                 | fail-closed evidence gate               | `QFAI-UIE-001/002` の blocking 条件                      |
| TC-08 | design contracts の canonical file set は `exploration-brief.yaml`, `evaluation-rubric.yaml`, `evaluator-calibration.yaml`, `selected-direction.yaml`, `design-system.yaml` とする | exploration-first downstream を固定する | `QFAI-DCON-*` validator 対象                             |
| TC-09 | UI contracts は `.qfai/contracts/ui/*.yaml` を canonical set とする                                                                                                                | screen contract truth source を固定する | downstream evaluator / validator の入力制約              |
| TC-10 | `qfai prototyping` CLI と removed runtime/full-harness engine を public contract として再導入しない                                                                                | skill-first posture を維持する          | public interface の制約                                  |
| TC-11 | breakthrough trigger は `allReviewerAxesPerfect100`, score delta, diff lines を使う deterministic な機械判定とする                                                                 | 再現性と監査性を保つ                    | `.qfai/evidence/breakthrough.json` が必要                |
| TC-12 | design system は discussion 初期入力ではなく、prototyping の winner direction から抽出する                                                                                         | 早期収束を防ぐ                          | discussion / SDD / prototyping の責務分離                |

## Operational Constraints

| ID    | Constraint                                                                                                              | Rationale                        | Impact                                   |
| ----- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ---------------------------------------- |
| OC-01 | `/qfai-prototyping` は唯一の public prototyping path とする                                                             | 誤用防止                         | CLI surface の単純化                     |
| OC-02 | `/qfai-verify` は `qfai validate --fail-on error` と review artifact を completion gate に含める                        | reviewer gate を必須化           | verify の制約                            |
| OC-03 | downstream validate は discussion artifact の存在で contract 欠落を代替してはならない                                   | fail-open 防止                   | contract 欠落は error                    |
| OC-04 | historical wording は `07_Decisions.md`, `09_delta.md`, `_policies/08_Decisions.md`, `_policies/10_delta.md` にのみ残す | active layer の誤読防止          | active 01..06/10 の整合制約              |
| OC-05 | prototyping は初期 funnel `5->3->2->1` と polish loop を持ち、停滞時は breakthrough branch 2 本を強制比較する           | exploration-first quality system | branch planner / plateau detector の制約 |

## Business Constraints

| ID    | Constraint                                                | Rationale           | Impact                          |
| ----- | --------------------------------------------------------- | ------------------- | ------------------------------- |
| BC-01 | drift recovery は upstream owner skill rerun を必須とする | SSOT 修正責任を保つ | downstream での場当たり修正禁止 |
| BC-02 | breaking changes は delta と migration expectation を伴う | ユーザー説明責任    | 文書管理の制約                  |

## CHG-003 (Assistant-layer Recut + Work-log Surface, 2026-05-22)

### Technical Constraints

| ID    | Constraint                                                                                                                                | Rationale                                                                     | Impact                                                                                                                  |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| TC-65 | `.qfai/assistant/` の top-level entry は `constitution/` / `manifest/` / `catalog/` / `process/` / `agents/` / `skills/` の 6 つに限定    | layer 概念を構造で強制                                                        | `assistantAssets.ts` が enumerated 以外を reject                                                                        |
| TC-66 | `.qfai/steering/` は project-root に置き `.qfai/assistant/` 配下に置かない                                                                | work-log は per-project surface であり global asset ではない                  | distributed surface (`package.json#files`) に含めない / `.gitignore` 既定 exclude                                       |
| TC-67 | `packages/qfai/src/core/paths/assistantPaths.ts` を assistant-path SSOT module として独占                                                 | path 漂流防止                                                                 | 文字列 literal `assistant/(steering\|manifest\|instructions\|catalog\|constitution\|process)/` を SSOT 外で lint reject |
| TC-68 | 旧 layout (`instructions/`, `steering/`, `manifest/`) は 1 minor release の間のみ validator が受理し `D-DEPRECATED-PATH` warning を発する | 既存 consumer の breaking を回避                                              | sunset version を migration memo に明記し、`qfai validate --report` の warning text に含める                            |
| TC-69 | 全 `qfai-*` SKILL.md が `project_memory:` block を持ち、読む layer を宣言する                                                             | skill ごとの load 範囲を構造化                                                | `constitution/` は always-loaded で列挙不要、宣言外の path read は validator が reject                                  |
| TC-70 | `kind: decision` entry の promotion は manual (自動 promote しない)                                                                       | per-spec `07_Decisions.md` SSOT の dilution 防止                              | promote 完了は (a) `07_Decisions.md` row 追加 + (b) entry archive + (c) `promoted-to` back-ref の 3 条件                |
| TC-71 | Reviewer Gate `R-*` finding は `justification:` 非空が必須                                                                                | heuristic な drift 検査を advisory-failing にする (mute と hard-block の中間) | `justification:` 欠落 finding は `qfai validate` が reject                                                              |

### Operational Constraints

| ID    | Constraint                                                                                                                                                                   | Rationale                                       | Impact                                                               |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | -------------------------------------------------------------------- |
| OC-50 | implementing PR は file relocate (25+ files) + path SSOT module + validator/CLI 更新 + SKILL.md 更新 + tests + migration memo を atomic に commit                            | 部分移行はバリデータ曖昧化を招く                | CI は partial moves を reject                                        |
| OC-51 | `.qfai/steering/` は `.gitignore` 既定 exclude。project は override で opt-in 可能                                                                                           | accidental secret commit 防止                   | seeded README で commit-by-default vs opt-out 選択を案内             |
| OC-52 | 配布物 (`packages/qfai/package.json#files`) には `assets/init/.qfai/steering/{README.md,.gitkeep,_templates/entry.md}` のみ含め、それ以外の `.qfai/steering/*.md` は含めない | user data を distributed surface に混入させない | layer 3 smoke test (`distributedSurfaceLeakage.test.ts`) が enforces |
| OC-53 | migration memo (`process/migrations/v<X.Y.Z>-assistant-layer-recut.md`) は commit 後 edit せず、修正は follow-up memo で対応                                                 | ADR-style immutability practice                 | post-merge 編集は CI 警告 (hard-fail ではない)                       |

## CHG-005 (qfai-prototyping defect remediation, 2026-05-24)

### Operational Constraints

| ID    | Constraint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Rationale                                                                                                                                                                                                                                                                                          | Impact                                                                                                                                                                                                                                                                                                                 |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OC-60 | One-minor-release deprecation window: validators / probes / readers MUST accept BOTH the old form (legacy `playwright-cli` wrapper, legacy `prototyping.json` shape, legacy `.qfai/output/validate.json` path, legacy `iterate-plan.json` without Capture Contract) AND the new form during the window opened in qfai 1.9.1. Sunset = qfai 1.10.0; from sunset onwards, old forms emit `error` instead of `warning`.                                                                                     | NFR-0111: downstream consumers on qfai@1.8.10 (the four reporter projects in SRC-0001..0004) MUST be able to upgrade incrementally without hand-patching every artifact in a single commit. CHG-003's path-recut precedent already established the one-minor cadence as the minimum viable window. | All `D-DEPRECATED-PROBE` / `D-DEPRECATED-PATH` / `D-DEPRECATED-SCHEMA` finding severities are window-aware. CI lint stays green on legacy artifacts during 1.9.x; CI must flip to error-fail at the 1.10.0 boundary. Implementers MUST not delete the legacy code paths inside the window — only mark them deprecated. |
| OC-61 | Migration memo immutability: once `.qfai/assistant/process/migrations/<pinned-version>-prototyping-defect-remediation.md` is committed, edits trigger a Reviewer-Gate `warning` (mirrors the ADR-style precedent established by the `v1.4.27-atdd-alignment.md` memo and re-affirmed by CHG-003 OC-53). Substantive corrections MUST be authored as a follow-up memo (`<later-version>-prototyping-defect-remediation-followup.md`) rather than rewriting the original.                                  | NFR-0109: migration memos are referenced by external recovery instructions handed to downstream consumers; their content must remain stable as a citation target. Rewriting the original silently breaks any quoted recovery step in downstream worklogs.                                          | Reviewer-Gate gains an advisory finding for post-merge edits to `process/migrations/*.md`. Hard-fail is not introduced (typo-fix latitude preserved); the warning forces explicit `justification:` per existing TC-71.                                                                                                 |
| OC-62 | Documentation drift bound after sunset: `qfai validate` MUST emit `W-SKILL-DOC-BROKEN-REF` (warning during 1.9.x, error from 1.10.0) when any `qfai-*` SKILL.md / `references/*.md` references a path, finding code, exit code, schema field, or CLI flag that no longer exists in the current layout. Coverage MUST include the four references that triggered CHG-005 (`/qfai-verify` reference in `qfai-prototyping/SKILL.md` Step H, `.qfai/output/validate.json` in `references/handoff.md`, etc.). | NFR-0108: the four reporter projects each independently lost time chasing documentation that named non-existent paths and obsolete skills. Without an automated drift check, the same class of defect will reappear with every CLI rename.                                                         | `qfai validate` lane scans skill markdown for known finding/path/flag/exit-code tokens and cross-checks against the live validator / CLI surface. Stale references that are inside the deprecation window stay as warning; after sunset they hard-fail builds.                                                         |

## CHG-006 (Second-Wave Defect Remediation, v1.9.2, 2026-05-27)

### Operational Constraints

| ID    | Constraint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Rationale                                                                                                                                                                                                                                | Impact                                                                                                                                                                                                                                                |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OC-63 | One-minor-release deprecation window (REQ-0169): every path or schema change in this pack ships with a backwards-compatible adapter. Validators / probes / readers MUST accept BOTH old and new forms during the window opened in qfai 1.9.2 and emit `D-DEPRECATED-*` (warning) as appropriate. Specific applications and their sunsets, corrected against what shipped: `surface_type`-absent specs (REQ-0163, `D-SURFACE-TYPE-MISSING`) — **sunset 1.10.0, enforced**; pre-mutation-log iterate emit (REQ-0165) — already `error` under `R-EVIDENCE-MUTATION-UNLOGGED`, so no window ever applied and wiring one would downgrade it; legacy ad-hoc handoff files (REQ-0161, `D-HANDOFF-LEGACY-FORMAT`) and string-only `primary_tasks` — **re-pinned to 1.11.0**, because neither ever emitted a finding during the 1.9.x window. Escalating those two at 1.10.0 would hand consumers a zero-length window, which is precisely what REQ-0169 — the NFR this row cites as its own justification — exists to prevent. Old-form acceptance is not removed at any of these sunsets: per OC-60 and `qfai-validate.md`, the reader is removed in the minor _after_ the sunset, and only the finding severity moves at it. Pre-emit-skeleton cycle-0 behavior (REQ-0150) is opt-in; default unchanged. | NFR (REQ-0169) mirrors the v1.9.1 `OC-60` precedent exactly: downstream consumers on qfai@1.9.1 must upgrade incrementally without hand-patching every artifact in one commit.                                                           | All `D-DEPRECATED-*` finding severities are window-aware. CI lint stays green on legacy artifacts during 1.9.x; flips to error-fail at the 1.10.0 boundary. Legacy code paths MUST NOT be deleted inside the window.                                  |
| OC-64 | Migration memo immutability (REQ-0170): once `.qfai/assistant/process/migrations/1.9.2-second-wave-defect-remediation.md` is committed, edits trigger a Reviewer-Gate `warning` (mirrors OC-61 / CHG-003 OC-53). Substantive corrections MUST be authored as a follow-up memo (`<later-version>-second-wave-defect-remediation-followup.md`) rather than rewriting the original.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Migration memos are citation targets in downstream recovery instructions; rewriting silently breaks any quoted recovery step in downstream worklogs.                                                                                     | Reviewer-Gate gains an advisory finding for post-merge edits to `process/migrations/*.md`. Hard-fail is not introduced (typo-fix latitude); the warning forces explicit `justification:` per TC-71.                                                   |
| OC-65 | Pack-location CI lane (REQ-0167): `packages/qfai/scripts/check-pack-locations.mjs` MUST lint staged/changed dirs for `review-*/` or `discussion-*/` directories outside the allowed roots (`tmp/` per `.agents/rules/temporary-files.md`, `.qfai/review/<ts>/`, `.qfai/discussion/<ts>/`) and be wired into `pnpm ci:lint`. Misplaced packs are rejected with `R-PACK-LOCATION-DRIFT` referencing `.agents/rules/root-additions-policy.md` and the corrected path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | NFR (REQ-0167): structural enforcement of `root-additions-policy.md` rule 6, which was textual-only. Review packs landing at repo root or under arbitrary dirs (the recurring drift) are caught at PR time rather than in manual review. | CI lint job gains the pack-location lane. Scope = staged/changed dirs only (not a full-tree walk) so legacy packs on unrelated PRs are not re-flagged (per DR-0274).                                                                                  |
| OC-66 | Envelope-deviation audit record + autopilot governance (REQ-0158 / REQ-0160): `AskUserQuestion` invocations naming a four-context envelope-deviation (skill envelope / architectural decision / rejected-option re-adoption / scope expansion, per DR-0270) MUST write `.qfai/evidence/decisions/<ISO8601-ts>.json`; every SKILL.md MUST carry a `## Default Autopilot Policy` section (DR-0269) and `/qfai-sdd` MUST NOT widen the auto-decide bucket beyond the policy prototype. `R-AUTOPILOT-POLICY-MISSING` (error) enforces presence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Removes the 3–6 avoidable `AskUserQuestion` per session while preserving an audit trail for envelope deviations; auto-deciding triage / destructive / version / scope operations stays forbidden.                                        | `qfai validate` / Reviewer Gate scan SKILL.md for the policy section; `.qfai/evidence/decisions/` is tracked in version control (governance record; negated in the managed `.gitignore` block, unlike the regenerable `.qfai/evidence/prototyping/`). |

## CHG-007 (Layered CI Test Scaffold Adoption, 2026-08-05)

### Operational Constraints

Derived from discussion-20260804173914356 `09_Constraints.md` (DTC-1..28, OC-1..9) and `07_NFR.md` (NFR-0001..0016). Each row was verified against the tree at the pack's measured commit; the guard / script paths named below are the enforcement surface.

| ID    | Constraint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Rationale                                                                                                                                                                                                                                                                                                                                                                         | Impact                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| OC-67 | Action-pin trailer placement (DTC-2 / DTC-22 / DTC-27): `packages/qfai/scripts/check-no-internal-version-leakage.sh` is **comment-blind and pragma-free**, so a conventional version-carrying pin trailer (`# v6.1.0`) in a **shipped** file fails the guard. The version therefore goes in the **step `name`**, never in a comment beside the pin. Bare full-SHA pins carry no version marker and are always legal. QFAI's own `.github/**` is outside the distributed surface, so a readable trailer stays legal there only.                                                                                                            | Verified by probe: the guard matches a leading-`v` dotted marker recursively over every `package.json#files` path; a bare major-version tag does not match. Layer 1 (`lint-shipping.ts`) skips YAML comment lines before its runtime rules, so it cannot backstop this.                                                                                                           | Any shipped-YAML pin rule must be evaluated **before** the comment skip or as a dedicated shipped-YAML rule. Ordering obligation: the pre-build rule and the asset-test co-change (DTC-26 — the asset test currently asserts floating major refs in the shipped workflow) land in the same change or earlier.                                                                                          |
| OC-68 | Shipped `.github/` allow-list (DTC-1 / DTC-15): `scripts/verify-pack.mjs` permits only `workflows` as an immediate child of the shipped `.github/` and **throws on any other child**. A shipped `actions/` directory is a hard pack failure, so **composite-action templates cannot ship**. Additional workflow **files** under `workflows/` are unconstrained and need no allow-list widening.                                                                                                                                                                                                                                           | The allow-list exists to keep dependency-bot config, issue templates and future GitHub config directories out of the tarball.                                                                                                                                                                                                                                                     | Deduplication inside a shipped file must use in-file repetition, not a composite action. The asset test's deny-list disagrees with the pack script in both directions, so "fixing the test" unblocks nothing — `pnpm verify:pack` is the binding gate.                                                                                                                                                 |
| OC-69 | Create-only install (DTC-4): `qfai init` copies the root asset tree with `force: false` / `conflictPolicy: "skip"` (`packages/qfai/src/cli/commands/init.ts`), so install **never refreshes** an already-installed workflow; only the assistant-skills tree is force-overwritable. Any refresh or prune behaviour is gated behind the shipped-workflow ownership contract with its provenance record, and must reuse the existing copy / prune primitives rather than growing a parallel implementation.                                                                                                                                  | This is an undocumented call-site literal, not a recorded decision — so it is a blocker on expanding the shipped set, not a caveat: ship a defect and every existing adopter keeps it permanently.                                                                                                                                                                                | Expanding the shipped surface multiplies permanent adopter drift. The drift channel is **detection first** (advisory finding on the diagnostic surface, exit code unchanged); the overwrite half stays deferred. A _declined_ file must be excluded from the copy set **before** the copy runs — create-only alone does not cover it, because the file is absent and would simply be re-written.       |
| OC-70 | Assistant-tree SSOT direction (DTC-20): `.qfai/assistant/**` is a generated byte-mirror synced from `packages/qfai/assets/init/.qfai/assistant/**` by `scripts/sync-init-to-root.mjs`. Any new or edited assistant file (including the layer-to-CI-lane mapping in a sibling catalog file) MUST be authored in the **asset tree** and mirrored; editing the repository-root copy directly is reverted by `pnpm sync:ssot` and fails `git diff --exit-code .qfai/ qfai.config.yaml` inside `pnpm ci:gate`.                                                                                                                                 | The root tree is generated, not authored. The catalog has no closed file-set assertion, so vocabulary is unconstrained — but placement is not.                                                                                                                                                                                                                                    | Adding a catalog file is a two-place change plus a sync run. Editing the root copy alone produces a red gate whose message points at a diff rather than at the mistake, so the direction must be stated in the spec rather than discovered.                                                                                                                                                            |
| OC-71 | Contiguous active spec range (DR-0275): `validateSpecSplitByCapability` derives its expected spec set **positionally** from the CAP count — each CAP's list **index** is mapped to the spec directory at that ordinal position (`packages/qfai/src/core/validators/specSplitByCapability.ts`), so actual CAP numbers are never read. The active spec range MUST therefore stay a gap-free ordinal sequence, and **any** reserved gap ID MUST be re-numbered once the CAP count reaches its ordinal. Where the gap sat when it was reserved — inside the range or past its end — is irrelevant, because the expected set only ever grows.  | A positional 1:1 gate and a reserved gap ID are mutually unsatisfiable from the moment the CAP count reaches that ordinal: with the gap held, an Nth capability has no legal name (the reserved ID violates the reservation; the next ID raises `QFAI-SPLIT-103` + `QFAI-SPLIT-104` simultaneously, both `error`). A trailing reservation only defers this; it does not avoid it. | Binds `11_Slice-Policy.md` §ID 安定性ルール 5 and §ギャップポリシー. Trailing gaps are **not** exempt: `spec-0017` was reserved as a trailing gap and detonated at the very next capability addition (DR-0275). `OQ-0023` tracks making the gate ID-based so the "leave a gap" default stops being a trap.                                                                                             |
| OC-72 | Gate placement is effective, not nominal (DTC-18 / NFR-0014): every gate introduced by this work MUST run on a pull request — from the lint aggregate or the test matrix. A gate placed in the release-only gate aggregate (`pnpm ci:gate`, invoked only by the release workflow) blocks **no** pull request. The leakage guard is likewise a separate workflow step, not a member of the lint aggregate.                                                                                                                                                                                                                                 | Historical composition put the asset-mirror diff behind the release workflow. A check nobody's pull request runs is documentation, not a gate.                                                                                                                                                                                                                                    | The shipped-set structural contract gate and the workflow-hygiene lane both land in the lint aggregate or the matrix. Verification is the gate's invocation path plus a planted violation turning a pull request red.                                                                                                                                                                                  |
| OC-73 | Required-status-context obligation preservation (DTC-28 / OC-4): the repository's only required status check is the job named `build`. Keeping the **name** is necessary but not sufficient — the job MUST also stay **unconditional** and MUST keep performing, or `needs:`-depend on jobs that perform, the enumerated verification set it performs today, with no item weakened by `continue-on-error`. No job carrying a required status context may be skippable while it carries it.                                                                                                                                                | Two quiet failure modes: the name survives while the substance moves out (green over almost nothing), and the job becomes skippable (a skipped job reports **success**, handing branch protection green for a job that never ran).                                                                                                                                                | Binds any producer/verifier split and any change-derived selection rule. Required-check reassignment is a repository-settings action no agent takes; it is a recorded hand-off (`OQ-0022`), and until it lands the documentation-only floor is 4 executed instances rather than 3.                                                                                                                     |
| OC-74 | Release-workflow filename immutability (DTC-10): the release workflow's **filename** is bound to npm trusted publishing by repository and filename. It is not renamed, moved or deleted by any phase; contents may be hardened.                                                                                                                                                                                                                                                                                                                                                                                                           | Platform binding outside the repository.                                                                                                                                                                                                                                                                                                                                          | No test or guard detects a violation — the failure surfaces at the next publish. Hardening work must treat the filename as frozen input.                                                                                                                                                                                                                                                               |
| OC-75 | No cross-file reference inside the shipped set (DTC-25): dependency edges cannot cross workflow files, so an aggregate verdict job must be **co-located** with the jobs it aggregates. No shipped file may reference another shipped file: create-only install offers no repair path, so an absent target turns the referencing workflow into a parse error. Forbidding the reference is what keeps a partial install merely _incomplete_. Layer separation is therefore expressed as separate **jobs inside** the orchestrator file, while the shipped set still spans several independent files.                                        | Platform semantics plus create-only install. A workflow skipped by a path filter reports nothing to branch protection rather than success, so path filters alone cannot satisfy a required check.                                                                                                                                                                                 | Both statements hold simultaneously: multi-**file** shipped set, layer separation as **jobs**. Neither licenses the other reading. Own-CI layer separation likewise stays inside the existing file so no unconfigurable check name is created.                                                                                                                                                         |
| OC-76 | The layer vocabulary does not grow (NFR-0015 / DTC-8 / DTC-9 / DTC-24): no new test layer, layer token or layer heading enters any file the layer-policy loader reads (the layer catalog and its legacy fallback). The parser extracts every layer token and heading from those files, so even explanatory prose can trip the vocabulary warning. The layer-to-CI-lane mapping lands in a **sibling** catalog file that the loader does not read, cross-linked from the layer catalog, and must not read as activating per-level routing the catalog marks as not enforced.                                                               | Verified by probe: a hyphenated prose phrase beginning with the layer prefix extracts as a token and trips the warning; a sibling file is invisible to the parser. Extending the built-in token set to legalize CI vocabulary is rejected.                                                                                                                                        | The vocabulary-warning occurrence count MUST be unchanged before and after every change, and the built-in token set untouched. Combined with OC-70, the sibling file is a two-place authored change under the mirror gate.                                                                                                                                                                             |
| OC-77 | Adopter cost does not rise on install (NFR-0016 / NFR-0009): a freshly initialized adopter that declares none of the optional layer-named test scripts executes **zero shipped test lanes** and exactly **one installing job** (the validate lane that exists today). Every added shipped lane is inert by default — declared but skipped by a false condition until the corresponding script exists or the opt-in variable is set. The orchestrator's detection and verdict jobs run unconditionally by design; they install nothing and each carries its own timeout.                                                                   | An always-run verdict is what keeps a zero-work pull request green, so it is bounded rather than eliminated. A shipped-template change must never require an adopter action to keep working.                                                                                                                                                                                      | The whole adopter cost delta is two short orchestration jobs. An adopter tree created by an older QFAI and never re-initialized keeps passing validate; release notes state that a shipped-template change reaches **new installs only**.                                                                                                                                                              |
| OC-78 | Failure direction is decided by the substitution test, not by cost (NFR-0013): fail **open** where the degraded behaviour is a **superset** of the work the correct input would have selected (failed diff, shallow clone, absent base ref, missing optional script, absent Node version file) and annotate it. Fail **closed** where degrading would **substitute** an input the lane's judgement depends on — shipped package-manager resolution is the one instance in the shipped set, because nothing installs and any continuation reports a result it never computed.                                                              | Stating this as a cost test lands on the wrong answer: a failed install also "costs only runner minutes". The verdict job and the failure threshold never degrade to success. A recorded decision removed a default fallback pack for exactly this reason.                                                                                                                        | A shipped template that fails closed **outside** the named class is rejected in review, and so is a step that fails open where degrading would substitute an input. Any change to the verdict job's failure semantics is blocking-review.                                                                                                                                                              |
| OC-79 | Guard integrity and the three-site pattern SSOT (NFR-0005 / DTC-3): no guard is weakened to make this work land. Any change to the distributed-surface forbidden-pattern set — **narrowing or additive, including binding an existing rule to a new target** — moves all three code expressions (`lint-shipping.ts`, `check-no-internal-version-leakage.sh`, `distributedSurfaceLeakage.test.ts`) plus `.agents/rules/distributed-surface.md` in **one** pull request containing **zero** template edits. Preference order: satisfy the guard as written, then adjust the convention, then narrow.                                        | A partial update leaves three layers disagreeing and whichever runs first decides. Isolating the guard change is what makes it reviewable on its own terms. The additive case is explicitly in scope because the shipped-YAML pin rule is additive.                                                                                                                               | New finding codes must also honour the rule-identifier grammar (DTC-17): a letters-only prefix plus exactly three digits, registered in the validate command's expectation table, and not colliding with the bare `R-` lint-aggregate namespace or the diagnostic's dotted lowercase scheme.                                                                                                           |
| OC-80 | No cost or parallelism claim ships on argument (OC-7 / NFR-0001 / NFR-0002 / NFR-0004): each is accepted or rejected on captured before-and-after numbers, and a measured negative result is a legitimate outcome that is recorded rather than retried until it agrees. Because the evidence tree is version-control-ignored, the numbers MUST also be quoted where a reviewer can read them — the pull-request description and the spec's decision record. Parallelism changes land one runner project per pull request, largest first, with three consecutive green aggregate results before merge, and no retry setting is introduced. | A baseline that exists only in an ignored directory is unreviewable. Retries would mask exactly the filesystem races that parallelism tuning risks introducing.                                                                                                                                                                                                                   | Build-artifact reuse is satisfied either by landing the reuse **or** by recording a measurement showing a wall-clock regression and keeping the rebuilds. Worker counts are a hypothesis to confirm, not a constant; revising the user's declared starting value requires the user's sign-off.                                                                                                         |
| OC-81 | Adopter-file identity is decided by the name lists plus provenance, never by the prefix (`CLI-WFSET`): the shipped `qfai-` filename prefix is a **reservation notice, not a selector**. The write set is the enumerated set of names QFAI currently ships and the prune set the enumerated set a previous version shipped; a prefix glob MUST NOT be used for either. Adopter-side file state is the **closed 5-value enum** `absent` / `adopter-owned` / `installed` / `modified` / `declined`, and no consumer may introduce a sixth state or collapse two into one.                                                                    | A prefix match is not evidence of ownership — an adopter may legitimately author a similarly-named file, and claiming it on refresh takes their work. Provenance is what makes `absent` and `declined` distinguishable, which is the difference between "install it" and "the adopter deliberately removed it".                                                                   | Binds the install path, the prune path and the diagnostic's per-state disposition to one vocabulary. Existing precedent to follow, not to re-invent: the only prefix-scoped pruner in the tree today covers command / prompt / skill integration directories, not the workflows directory. A permanent blind spot remains for adopters who installed before the provenance record existed (`OQ-0026`). |
